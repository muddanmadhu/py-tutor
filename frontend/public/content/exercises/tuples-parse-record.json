{"slug":"tuples-parse-record","title":"Return a fixed-shape record","prompt":"Write `parse_record(line)` which takes a string like\n\n```\n2026-01-15,EMEA,4200\n```\n\nand returns a **tuple** `(date, region, revenue)` where `date` and `region` are strings and `revenue` is an `int`.\n\nSurrounding whitespace on any field must be stripped. Raise `ValueError` with the message `expected 3 fields` if the line does not have exactly three fields.","kind":"code","level":"intermediate","difficulty":0.35,"estimated_minutes":14,"xp_reward":35,"starter_files":{"main.py":"def parse_record(line: str) -> tuple[str, str, int]:\n    \"\"\"Parse \"date,region,revenue\" into a fixed-shape tuple.\"\"\"\n    # Your code here\n    ...\n"},"grader":"pytest","concept_slugs":["tuples"],"lesson_slug":"tuples-and-unpacking","hint_count":4,"hints_revealed":0,"solution_unlocked":false,"time_limit_minutes":null,"attempts":0,"best_score":0.0,"status":"not_attempted","options":null,"execution_plan":{"mode":"pytest","entrypoint":"main.py","stdin":"","pytest_args":["-v","--tb=short","--color=no","-p","no:cacheprovider"],"extra_files":{"test_record.py":"import pytest\n\nfrom main import parse_record\n\n\ndef test_returns_a_tuple():\n    result = parse_record(\"2026-01-15,EMEA,4200\")\n    assert isinstance(result, tuple), \"return a tuple, not a list or a dict\"\n    assert result == (\"2026-01-15\", \"EMEA\", 4200)\n\n\ndef test_revenue_is_an_int():\n    _, _, revenue = parse_record(\"2026-01-15,EMEA,4200\")\n    assert isinstance(revenue, int), \"revenue must be converted from text to int\"\n\n\ndef test_strips_whitespace():\n    assert parse_record(\"  2026-01-15 , EMEA ,  4200 \") == (\"2026-01-15\", \"EMEA\", 4200)\n\n\ndef test_wrong_field_count_raises():\n    with pytest.raises(ValueError, match=\"expected 3 fields\"):\n        parse_record(\"2026-01-15,EMEA\")\n\n\ndef test_too_many_fields_also_raises():\n    with pytest.raises(ValueError, match=\"expected 3 fields\"):\n        parse_record(\"a,b,c,d\")\n"},"timeout_seconds":10.0},"hints":[{"level":1,"text":"`line.split(\",\")` gives you the fields as a list.","penalty":0.05,"is_last":false},{"level":2,"text":"Check `len(fields)` before unpacking, so you can raise your own error rather than letting ValueError escape with a different message.","penalty":0.05,"is_last":false},{"level":3,"text":"`str.strip()` removes surrounding whitespace. You need it on every field, not just the first.","penalty":0.05,"is_last":false},{"level":4,"text":"Shape: split, check the length, then `date, region, revenue = (f.strip() for f in fields)` — and convert revenue with `int(...)` before returning the tuple.","penalty":0.05,"is_last":true}],"solution":{"files":{"main.py":"def parse_record(line: str) -> tuple[str, str, int]:\n    \"\"\"Parse \"date,region,revenue\" into a fixed-shape tuple.\"\"\"\n    fields = line.split(\",\")\n    if len(fields) != 3:\n        raise ValueError(\"expected 3 fields\")\n    date, region, revenue = (field.strip() for field in fields)\n    return (date, region, int(revenue))\n"},"explanation":"The length check comes before unpacking so the error message is ours rather than Python's. Stripping every field in one generator expression avoids three near-identical lines, and the int conversion happens at the boundary — once, here, rather than at every use site."},"misconception_rules":[{"pattern":"test_returns_a_tuple","misconception":"list-vs-tuple"},{"pattern":"test_revenue_is_an_int","misconception":"type-mismatch"}],"grader_config":{}}